---
const { projectTextProps, positionShape } = Astro.props;
console.log(positionShape)
---

<div class="container">
    <div class="text">
        <h2 class="title">{projectTextProps.title}</h2>
        <span>{projectTextProps.techstach}</span>
        <span>{projectTextProps.description}</span>
    </div>
    <img src={projectTextProps.shape} alt="shape">
</div>
<style>
    @import url('https://fonts.googleapis.com/css2?family=Albert+Sans:ital,wght@0,300;1,300&family=Poiret+One&display=swap');

    .container {
        position: relative;
        width: 100%;
        height: 100%;
        display: flex;
        gap: 1rem;
        font-family: "Albert Sans", serif;
        font-optical-sizing: auto;
        font-weight: 200;
        justify-content: center;
        align-items: center;
        color: #a9a9a9;
        background: #f1f1f1;
        overflow: hidden;
    }

    .text {
        position: relative;
        z-index: 2;
        display: flex;
        flex-direction: column;
        align-items: start;
        gap: 1rem;
        letter-spacing: .1em;
    }

    .title {
        font-size: 1rem;
        font-weight: bold;
        color: #3a3a3a;
    }

    span {
        font-size: .8rem;
        width: 80%;
    }
</style>
